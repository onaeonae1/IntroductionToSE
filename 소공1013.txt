real time의 역할

display & speaker controller:
먼저 mod에 따라 읽어올 data store들에서 읽어올 값을 결정
그거를 7개의 배열에 각각 저장
7개의 배열은 display 7개에 일대일 대응

show/alarm까지가 한 주기

filter controller: tick을 따로 주기
자기 혼자 tick마다 
--> tick이 2개면 멀티스레드 해야하는데..??
backlight의 값을 읽어 on일 경우 2초간 backlight을 trigger
backlight는 누가 끄는지 --> real-time에서 관리해야 함 (논의 필요)
